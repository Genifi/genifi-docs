# GitBook Git Sync configuration.
#
# Kept alongside the MkDocs build so the GitBook space keeps working as a
# fallback. Both toolchains read the same files under docs/.
root: ./docs

structure:
  readme: README.md
  summary: SUMMARY.md
